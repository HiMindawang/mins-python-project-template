# mins-python-project-template
All my love to pygumby

## How to use
- Fork this repository and use it as a starting point for new projects
- In `pyprojects.toml`, update the `name` to your project's name
- If your project requires packages from PyPI, run `uv add <package name>`
- Run `uv run main.py` to run main.py
